version: '3.8'

services:
  shopping-list:
    build: .
    container_name: shopping-list-app
    ports:
      - "8081:80"
    volumes:
      - shopping_list_data:/usr/src/app/data
    restart: unless-stopped

volumes:
  shopping_list_data:
